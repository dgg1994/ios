<template>
  <div class="coming-card">
    <h2>{{ $route.meta.title || "功能" }}</h2>
    <p>该页面对应的写入接口尚未迁入 Java <code>26-admin-server</code>（应用打包 / 权限矩阵 / 飞机模版）。列表类页面已接到现有 JSON 接口。</p>
  </div>
</template>
